# REVIEW: MCP-17 by codex

- change-id:  MCP-17
- reviewer:   { pilot: codex, model: codex/gpt-6-astra }
- timestamp:  2026-09-11T11:04:12Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN
- resource-efficiency:     PASS
- business-logic-vs-spec:  CONCERN
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S2
  dimension: correctness
  issue: Inherited stamps still consume a legitimate call's budget while the session is younger than the auth slice.
  evidence:  sugra_api_mcp/server.py:155-175
  required_fix: Establish request ownership independently of stamp age: carry the timestamp with the current request, or exclude the session's inherited stamp. With SUGRA_TOOL_DEADLINE=2 an inherited stamp aged 1.8s leaves about 0.2s and a 0.3s tool times out; at elapsed == total <= 15 the call is refused before dispatch, and retrying past that boundary succeeds, which contradicts the new hint.
- severity: S3
  dimension: business-logic-vs-spec
  issue: The stale-stamp fallback bypasses the nonpositive-budget guard, so a configured 0 or negative budget reaches asyncio.timeout.
  evidence:  sugra_api_mcp/server.py:160-201; sugra_api_mcp/config.py:68; sugra_api_mcp/auth.py:532-534
  required_fix: Reject nonfinite or nonpositive configured budgets and enforce the deadline guard after every budget-selection branch.
- severity: S3
  dimension: maintainability
  issue: The rewritten test does not establish the no-floor invariant it is named for.
  evidence:  tests/test_tool_deadline.py:132-149
  required_fix: Assert the timeout argument against the remaining budget under controlled time. A mutation replacing the subtraction with max(0.75, total - elapsed) still passed the test, because its upper bound of 2.5s was wider than the value it meant to pin.

## summary
Round 1 on head 8b51980. The patch removes the permanent denial but not the wrong subtraction: a session younger than the auth slice still loses budget, and the refusal survives at the equality boundary. Verified five deadline tests pass and no zero or negative deadline reaches asyncio.timeout for a finite positive total. Runtime probes used local MCP 1.27.0 and Python 3.14.3; the hosted MCP 1.23.3 was not executed. No files were modified by the review.
