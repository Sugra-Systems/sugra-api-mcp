# REVIEW: MCP-17 by codex

- change-id:  MCP-17
- reviewer:   { pilot: codex, model: codex/gpt-6-astra }
- timestamp:  2026-09-11T11:13:15Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN
- resource-efficiency:     PASS
- business-logic-vs-spec:  CONCERN
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S2
  dimension: business-logic-vs-spec
  issue: The configured end-to-end budget becomes two sequential budgets.
  evidence:  sugra_api_mcp/server.py:160-165; sugra_api_mcp/auth.py:539-567
  required_fix: Preserve the configured end-to-end limit with call-owned timing, or revise the budget contract and the client timeout assumption together. With SUGRA_TOOL_DEADLINE=0.5, a 0.3s auth followed by a 0.3s tool returned success after 0.618s in an executable probe; at the default, cold auth plus dispatch approaches 55s while the envelope still claims an end-to-end limit.
- severity: S3
  dimension: correctness
  issue: Invalid budgets do not refuse startup as the new guard promises.
  evidence:  sugra_api_mcp/config.py:69-89; sugra_api_mcp/__main__.py:37-101; sugra_api_mcp/auth.py:539
  required_fix: Validate the deadline before starting either transport and test rejection through the startup entry point. With SUGRA_TOOL_DEADLINE=0 the HTTP startup probe reached uvicorn, /health returned 200, and an authenticated request returned an unstructured 500 because the ValueError was raised inside AuthMiddleware.
- severity: S3
  dimension: maintainability
  issue: A 7s capture cannot detect a floor below 7s.
  evidence:  tests/test_tool_deadline.py:144-158
  required_fix: Capture the dispatch timeout argument with a configured budget below the prohibited floor, such as 0.25s, and assert exactly that. Replacing asyncio.timeout(deadline) with asyncio.timeout(max(0.75, deadline)) passed all six deadline tests.

## summary
Round 2 on head 246f619. Round-1 F1 and F2 are closed: inherited stamps no longer affect dispatch and the parser rejects nonfinite and nonpositive budgets before either timeout. Three findings remain, the material one being that the end-to-end contract is no longer preserved once the subtraction is gone. Verified all four reviewed file hashes against the diff and 45 deadline, auth and auth-concurrency tests. Probes ran in memory on Python 3.14.3 with MCP 1.27.0; the hosted MCP 1.23.3 was not executed. No files were modified by the review.
