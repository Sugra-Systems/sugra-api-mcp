# REVIEW: MCP-19.1.1 by codex

- change-id:  MCP-19.1.1
- reviewer:   { pilot: codex, model: codex/gpt-6-astra }
- timestamp:  2026-09-12T07:52:10Z
- verdict:    APPROVE_WITH_CHANGES

## dimensions
- correctness:             CONCERN: The competing case pins identity but does not independently pin the original args.
- resource-efficiency:     PASS: No runtime changes or material resource impact.
- business-logic-vs-spec:  CONCERN: The anyio case verifies scope cleanup but omits the required identity and args checks.
- security:                N/A: Tests-only cancellation coverage.
- maintainability:         CONCERN: The competing case can fail when the initial loop wakeup is delayed beyond the budget.
- operational-cost:        N/A: No production operational changes.

## findings
- severity: S3
  dimension: business-logic-vs-spec
  issue: The anyio test does not enforce cancellation identity or args preservation.
  evidence:  tests/test_observability.py:1303-1322: The scope consumes cancellation without capturing the incoming and outgoing exception. Replacing the wrapper's bare raise with a fresh CancelledError(*original_cancel.args) only when dispatch_budget.get() is None leaves all 127 permitted tests passing.
  required_fix: Capture the exception object and a separate args snapshot inside the tool, catch it immediately outside the wrapper but inside CancelScope, assert identity and unchanged args, then bare-raise so the existing scope cleanup assertions still execute.
- severity: S3
  dimension: correctness
  issue: The competing-case args assertion cannot detect in-place alteration of the cancellation reason.
  evidence:  tests/test_observability.py:1220-1221,1282-1284: After asserting identity, both sides of the args comparison read the same object's current attribute. A mutant that sets original_cancel.args = ('altered-competing-reason',) only when the budget has expired and cancelling() > 1 passes all 127 tests.
  required_fix: Save exc.args alongside exc at the inner capture point and compare the outgoing args against that saved tuple.
- severity: S3
  dimension: maintainability
  issue: The competing test depends on waking within a 20 ms setup window and can fail under CI scheduling delays.
  evidence:  tests/test_observability.py:1272-1277: Injecting a 50 ms delay into the first blocking SelectorEventLoop selector call makes the unchanged test raise TimeoutError. The 30 ms budget fires before the external cancellation callback is registered after the 10 ms sleep.
  required_fix: Arrange cancellation callbacks synchronously after the budget is entered and before yielding, using controlled scheduling or a test clock; assert that the budget expired and both cancellation requests reached the task before propagation.

## summary
The prior S3 is partially closed. External cancellation pins identity and the supplied reason, and all three requested replacement mutants are killed. Anyio identity and args coverage remains incomplete, the competing args check is tautological, and its scheduling is not deterministic under delayed wakeups. These are S3 follow-ups; no runtime code changed.
