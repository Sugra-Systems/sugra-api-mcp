# REVIEW: MCP-19.1 by codex

- change-id:  MCP-19.1
- reviewer:   { pilot: codex, model: codex/gpt-6-astra }
- timestamp:  2026-09-12T07:08:40Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN: Competing external cancellation is still attributed to the budget.
- resource-efficiency:     PASS: Constant work per cancellation, with no added tasks or I/O.
- business-logic-vs-spec:  PASS: Both cancellation paths record failure; the configured timeout argument and budget envelope remain unchanged.
- security:                PASS: Both paths retain allowlisted codes and pinned attribute names and scalar types.
- maintainability:         CONCERN: Cleanup tests still permit loss of a prior ContextVar value and missing cleanup after a raised exception.
- operational-cost:        PASS: No additional spans, dimensions, dependencies or network calls.

## findings
- severity: S2
  dimension: correctness
  issue: Timeout.expired() proves the timer fired but does not prove it owns the cancellation being propagated.
  evidence:  sugra_api_mcp/observability.py:81-86; sugra_api_mcp/server.py:173-178: With a 0.03s budget, schedule external cancellation at timeout.when()-0.005 and block the loop for 0.06s. Both callbacks execute before the sleeping tool resumes. The wrapper observes expired()==True and cancelling()==2, records deadline_exceeded, then call_tool propagates CancelledError('external-owner') with cancelling()==1 instead of returning a timeout envelope. Reproduced with both Task.cancel and AnyIO CancelScope.cancel.
  required_fix: Publish cancellation ownership alongside timeout expiration and classify competing cancellation consistently with asyncio.Timeout's cancellation-count ownership check. Preserve the timeout argument and unchanged exception propagation. Add regressions where external cancellation and the budget both fire before the wrapper resumes, including AnyIO cancellation.
- severity: S3
  dimension: maintainability
  issue: The cleanup tests do not enforce restoration of the prior value or cleanup after an actual exception.
  evidence:  tests/test_tool_deadline.py:122-167: The cleanup test starts without a prior timeout and its tool-error case returns a dictionary rather than raising. Two independent in-memory mutations each passed all 138 permitted tests: replacing server.py:199 reset(budget_token) with set(None), and skipping that reset only when an ordinary exception propagates.
  required_fix: Seed a non-None prior ContextVar value and assert identity restoration in the dispatch context after success, budget timeout, external cancellation and an actual raised dispatch exception. Retain the returned-error case separately.

## summary
Both prior findings were accepted and none declined. Accepting them was justified, but neither is fully closed. Publishing the Timeout fixes both clock-based reproductions, including a probe where it expired with approximately 28ms remaining; expiration alone still misses competing cancellation ownership. The external-cancellation duration and ERROR assertions are fixed, but cleanup coverage remains incomplete. Ran the permitted suite: 138 passed on Python 3.14.3, MCP 1.27.0 and AnyIO 4.13.0. Inspected installed lowlevel/server.py:678-770, fastmcp/server.py:343-346, tools/tool_manager.py:81-93, tools/base.py:93-108 and utilities/func_metadata.py:93: dispatch directly awaits the tool in the same task and context. An instrumented in-memory session confirmed matching task and Timeout identities. Request tasks are created before this value is set; each dispatch replaces it and finally resets it without suspension. Current code restored a seeded prior value, including after a raised ToolError. AnyIO probes preserved exception identity, cancellation counts, shielding and scope handling with tracing enabled. A real in-memory fetch_data budget cancellation stamped both wrappers; external cancellation also stamped both. The allowlist and attribute contract tests passed. git diff --check passed and tracked files remained unchanged. Full-suite, ruff and post-deployment acceptance claims were not independently verified.
