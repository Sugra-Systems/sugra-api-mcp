# REVIEW: MCP-19.1 by codex

- change-id:  MCP-19.1
- reviewer:   { pilot: codex, model: codex/gpt-6-astra }
- timestamp:  2026-09-12T07:20:41Z
- verdict:    APPROVE_WITH_CHANGES

## dimensions
- correctness:             PASS: Current implementation preserves cancellation ownership, exception identity, counts, shielding and timeout translation in the exercised paths.
- resource-efficiency:     PASS: Constant-time bookkeeping adds no tasks, timers or network operations.
- business-logic-vs-spec:  PASS: Configured timeout argument and client envelope remain unchanged; both nested fetch_data spans receive the verdict.
- security:                PASS: Both cancellation codes are allowlisted; pinned attribute names and exact scalar types hold.
- maintainability:         CONCERN: Cancellation tests permit replacing the propagated exception and losing its arguments.
- operational-cost:        PASS: Changes use bounded values on existing telemetry dimensions.

## findings
- severity: S3
  dimension: maintainability
  issue: The tests do not enforce the promised unchanged cancellation propagation.
  evidence:  tests/test_observability.py:1125-1148,1176-1202 assert only CancelledError's class. An in-memory mutation replacing observability.py:442 bare raise with raise asyncio.CancelledError() passed all 140 permitted tests. Independent Task.cancel('external-owner') and AnyIO probes showed a different propagated exception with empty args instead of the original cancellation reason.
  required_fix: Add regressions asserting exception identity and arguments across the wrapper for Task.cancel and AnyIO cancellation, including competing budget cancellation. Assert cancellation counts and AnyIO scope handling remain unchanged.

## summary
Round 2's S2 ownership defect and S3 restoration gap are closed. Both findings were accepted, and the recorded reasoning is sound: the count comparison matches asyncio.Timeout's ownership check, and cleanup now restores a seeded value by identity after all five outcomes. Independently rerunning the expired-only, set(None), and skipped-exception-reset mutants produced 1, 5, and 1 failing tests respectively. The remaining S3 concerns propagation coverage, including the requested AnyIO regression. Installed MCP 1.27.0 source confirms request tasks are spawned before publication in lowlevel/server.py:678, followed by direct awaits through lowlevel/server.py:770,535, fastmcp/server.py:346, tools/tool_manager.py:93, tools/base.py:101 and utilities/func_metadata.py:93. Runtime probes confirmed unique budgets across concurrent dispatches and two sessions, prior-value restoration, and cleanup when cancellation is requested at set before try. Clock probes reproduced expired=True with now<when and external cancellation after the deadline before timer execution; both were classified correctly. Competing Task.cancel and AnyIO cancellation preserved identity and counts, and shielding matched tracing-disabled behavior. A real in-memory fetch_data call produced both completed deadline_exceeded spans with allowed attribute names and types. Ran all 140 permitted tests successfully on Python 3.14.3 with AnyIO 4.13.0, plus independent probes and git diff --check. HEAD remains 10cd3ea and tracked files are unchanged. Full-suite, ruff and post-deployment telemetry claims were not independently verified.
