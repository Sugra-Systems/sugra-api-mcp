# REVIEW: MCP-19 by codex

- change-id:  MCP-19
- reviewer:   { pilot: codex, model: codex/gpt-6-astra }
- timestamp:  2026-09-11T19:40:34Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN: The shared extractor drops legitimate timeseries recipe versions.
- resource-efficiency:     N/A: No performance measurements taken.
- business-logic-vs-spec:  PASS: Executed status mapping, precedence, partial-envelope classification and seven documented snapshot recipes at version 1.
- security:                CONCERN: Recipe names are allowlisted, but versions remain unrestricted numeric strings.
- maintainability:         PASS: Prior S3 is CLOSED; the 503 tool-name list mutation fails the exact-type assertion, and both original leak mutations fail.
- operational-cost:        N/A: Production telemetry and post-deployment acceptance were not measured.

## findings
- severity: S2
  dimension: security
  issue: Prior S2 is PARTIALLY CLOSED: the demonstrated unknown recipe names are rejected, but the required version allowlist is still missing.
  evidence:  sugra_api_mcp/tools/agent.py:85,92-95,142-144: mocked HTTP 200 through real SugraClient and get_snapshot with {"data":{},"error":"partial","recipe_version":"company_snapshot@123456"} emitted mcp.success=true and the unchanged recipe_version. Separate rejection tests also failed for company_snapshot@000000 and company_snapshot@2.
  required_fix: Allowlist complete approved recipe/version constants and omit unrecognized versions. Replace arbitrary-version acceptance tests with approved-version cases and add rejection regressions through real SugraClient and get_snapshot.
- severity: S3
  dimension: correctness
  issue: The snapshot-only allowlist and regex remove recipe attribution from every supported timeseries path.
  evidence:  sugra_api_mcp/tools/agent.py:76-95,142-144,215: executed the local plane's helpers/agent_timeseries.py:95 producer with mocked loaders, then passed its HTTP 200 envelopes through real SugraClient and get_timeseries. All four preservation tests failed for timeseries.price@1, timeseries.macro_series@1, timeseries.etf_flows@1 and timeseries.etf_monthly_flows@1. All four passed with origin/main's agent module loaded in memory.
  required_fix: Include the four approved timeseries version constants in the shared extractor's allowlist and add get_timeseries span regressions using actual plane envelope shapes.

## summary
Verified head f362464; all 147 permitted tests passed. In-memory mutations produced failures: shape-only recipe validation 4, any-string validation 15, tool-name list at 503 1, error-text list 30, URL leak 22. Both bool statuses were rejected, and pinned attribute checks passed for valid metadata through all three agent tools. Seven additional contract tests reproduced the findings. Plane verification used local source at a9837b7 with mocked loaders and HTTP transport, not production. Reread the full diff; git diff --check passed and tracked files remained unchanged.
