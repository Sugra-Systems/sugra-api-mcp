# REVIEW: MCP-24.3 by codex

- change-id:  MCP-24.3
- reviewer:   { pilot: codex, model: codex/gpt-6 }
- timestamp:  2026-09-12T12:02:29Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN: Flag parsing, raw inclusion, metadata availability and records_path wording differ from runtime.
- resource-efficiency:     PASS: Bare-name passthrough adds no processing overhead.
- business-logic-vs-spec:  CONCERN: PR B omits restrictions and precedence that determine which object fields projects.
- security:                PASS: No secrets or prohibited identities found in changed copy; compose retains explicit opt-in.
- maintainability:         PASS: The existing compose test requires the new variable and rejects baked values.
- operational-cost:        PASS: No new service or default widget activation.

## findings
- severity: S2
  dimension: correctness
  issue: The documented off condition excludes values that actually enable widgets.
  evidence:  sugra-api-mcp/docs/self-hosting.md:29-33 says any value other than the four lowercase spellings keeps widgets off. ui_widgets_enabled strips whitespace and lowercases first, so TRUE and padded on enable them.
  required_fix: State that matching is case-insensitive and surrounding whitespace is ignored before evaluating the four accepted values.
- severity: S2
  dimension: business-logic-vs-spec
  issue: PR B describes a broader records-list selection rule than the hosted server implements.
  evidence:  sugra-api-skills/plugins/sugra-api/skills/discover-and-call/SKILL.md:35 and plugins/sugra-api/skills/envelope-and-attribution/SKILL.md:29 describe the one list inside object data without the allowlist restriction. For data={custom:[1,2]}, limit=1 leaves both elements, with limit_applied=false and records_path=null.
  required_fix: Specify exactly one list-valued key among data, entries, events, history, items, observations, points, records, results, rows, series and timeseries; explain that zero or multiple candidates disable record-list selection and nested record lists are never truncated.
- severity: S2
  dimension: business-logic-vs-spec
  issue: PR B omits the field-selection exception that can remove the entire records list.
  evidence:  sugra-api-skills/plugins/sugra-api/skills/discover-and-call/SKILL.md:35 and plugins/sugra-api/skills/envelope-and-attribution/SKILL.md:29 say fields applies to records; the latter also promises sibling keys stay. With data={total:1,items:[{title:'a'}]} and fields=['total','title'], runtime returns data={total:1}, removes items and reports records_path=null.
  required_fix: Document own-object field-match precedence, qualify sibling preservation as applying to record-list shaping, and state that a projection matching no requested fields preserves its target.
- severity: S2
  dimension: correctness
  issue: PR B incorrectly describes the scope of include_raw.
  evidence:  sugra-api-skills/plugins/sugra-api/skills/discover-and-call/SKILL.md:35 and plugins/sugra-api/skills/envelope-and-attribution/SKILL.md:29 include include_raw among arguments applying to the records list. Runtime instead attaches the complete original payload under top-level raw, including envelope metadata and unbounded records, subject to the size cap.
  required_fix: Separate include_raw from limit and fields; describe complete original-payload inclusion and size-based omission.
- severity: S2
  dimension: correctness
  issue: The hosted qualifier is accurate for nested-list shaping but insufficient for a stdio reader.
  evidence:  sugra-api-skills/plugins/sugra-api/skills/discover-and-call/SKILL.md:35 qualifies nested-list shaping as hosted behavior but unconditionally promises meta.shaped.records_path. The stated stdio release baseline, 0.12.0, predates this metadata field.
  required_fix: Qualify records_path availability as hosted-server behavior too, and explicitly identify released stdio 0.12.0 as retaining the earlier shaping behavior.
- severity: S3
  dimension: correctness
  issue: A non-null records_path does not prove that field projection succeeded.
  evidence:  sugra-api-mcp/sugra_api_mcp/skills/envelope-attribution/SKILL.md:14 describes records_path as the list fields projected. With data={items:[{title:'a'}]} and fields=['missing'], runtime preserves the payload but still reports records_path='data.items'.
  required_fix: Describe records_path as the list bounded or checked for field matches, including unsuccessful matches; retain data, data.<key> and null cases.

## summary
Both requested heads and supplied diffs were verified. PR A's README correctly lists the twelve keys, selection rule, own-key precedence and unmatched-field preservation. A supplied limit on a selected list sets limit_applied=true even when no elements are removed. Compose, catalog and widget tests passed: 114 tests. PR A diff whitespace checks passed. Neither PR edits tool names, descriptions, schemas or annotations. All three reviewed skills are ASCII; changed copy contains no prohibited names, em dashes, emoji or secrets. No tracked files were modified. Hosted deployment and PyPI release status were treated as supplied premises, not independently tested.
