# REVIEW: MCP-25 by codex

- change-id:  MCP-25
- reviewer:   { pilot: codex, model: codex/gpt-5.6-luna }
- timestamp:  2026-09-13T14:32:53Z
- verdict:    APPROVE_WITH_CHANGES

## dimensions
- correctness:             PASS
- resource-efficiency:     PASS
- business-logic-vs-spec:  PASS
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S3
  dimension: maintainability
  issue: The regression suite still does not prove all claimed credential consumers and entry points.
  evidence:  tests/test_request_credentials.py:80-162,157-230 and __main__.py:76-80: coverage exercises call_endpoint and sugra_entity_screen, but not fetch_data, entity lookup, hosted agent tools, prompts, resources, the actual __main__ HTTP startup path, or mixed HTTP and stdio execution; the static scan only checks import text.
  required_fix: Add real-app tests for every get_client consumer, invoke the actual HTTP entry point, and run HTTP and stdio paths in one process with explicit transport isolation.
- severity: S3
  dimension: maintainability
  issue: Global client state makes the tests order-dependent and can leak async HTTP clients.
  evidence:  tests/test_request_credentials.py:49-55,59-69: the fixture overwrites the process-global shared client without closing or preserving a pre-existing client, while cleanup closes only a hard-coded subset of per-key clients and does not isolate the global client registry.
  required_fix: Snapshot and restore the client registries, close every client created by each test, and ensure the session manager and cached clients cannot leak across tests.

## summary
The previous S2 correctness finding is CLOSED: AuthMiddleware now marks each request, HTTP dispatch reads only that request scope, missing request state returns the keyless client, and stdio retains api_key_ctx and SUGRA_API_KEY behavior. The previous S3 finding remains OPEN because coverage is broader but still incomplete. The MCP v1.x transport attaches each POST message's Request to its dispatch metadata, does not batch JSON-RPC messages, and uses GET only for server-to-client SSE; see the Streamable HTTP transport and low-level dispatcher sources: https://raw.githubusercontent.com/modelcontextprotocol/python-sdk/v1.x/src/mcp/server/streamable_http.py and https://raw.githubusercontent.com/modelcontextprotocol/python-sdk/v1.x/src/mcp/server/lowlevel/server.py.
