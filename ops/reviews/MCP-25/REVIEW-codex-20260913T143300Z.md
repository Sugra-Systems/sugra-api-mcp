# REVIEW: MCP-25 by codex

- change-id:  MCP-25
- reviewer:   { pilot: codex, model: codex/gpt-5.6-luna }
- timestamp:  2026-09-13T14:33:00Z
- verdict:    APPROVE_WITH_CHANGES

## dimensions
- correctness:             PASS
- resource-efficiency:     PASS
- business-logic-vs-spec:  PASS
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S3
  dimension: maintainability
  issue: The integration suite does not prove the complete claimed credential-consumer surface.
  evidence:  tests/test_request_credentials.py:76-153 and 216-229 exercise only gateway.call_endpoint and sugra_entity_screen; the static scan does not execute fetch_data, hosted agent tools, prompts, resources, or every credential-consuming caller.
  required_fix: Add real-app parametrized coverage for every caller that can reach get_client, including hosted agent tools, resources, prompts, and no-request dispatch paths.
- severity: S3
  dimension: maintainability
  issue: Global MCP state and incomplete client cleanup make the tests order-sensitive.
  evidence:  tests/test_request_credentials.py:47-64 resets only _shared_client and closes only an explicit key list, while :76-86 uses the global server.mcp session manager; unexpected per-key clients can remain open or affect later tests.
  required_fix: Clear and close all clients created by each test, isolate or recreate the MCP session manager, and assert no client or session state remains after teardown.

## summary
The round-3 delta is behavior-neutral: the moved state write is after the timeout context and only follows successful resolution, so TimeoutError and AuthError paths cannot store a key; the lazy import can still propagate ImportError, but moving it inside this try does not change that failure class. In the supported v1 request path, request_ctx is populated from the current transport metadata, while GET SSE, notifications, and request-less flows have no attached request and fail closed through http_transport_ctx. See the official v1.13 implementation at https://github.com/modelcontextprotocol/python-sdk/blob/v1.13.0/src/mcp/server/lowlevel/server.py#L2597-L2623. No S1 or S2 defect is present. The two existing S3 test findings remain.
