# REVIEW: MCP-26.1.1 by codex

- change-id:  MCP-26.1.1
- reviewer:   { pilot: codex, model: gpt-5.6-luna }
- timestamp:  2026-09-13T20:40:32Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN
- resource-efficiency:     PASS
- business-logic-vs-spec:  PASS
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S1
  dimension: correctness
  issue: A malformed mapping result can make telemetry instrumentation change the tool result.
  evidence:  sugra_api_mcp/observability.py:516-519 calls result.get("scope") without a safety guard; a dict subclass can return server_busy for get("error") but raise for get("scope"), propagating instead of returning the tool payload.
  required_fix: Catch exceptions from scope extraction and treat the scope as absent, then add a regression test using a dict subclass whose get("scope") raises.
- severity: S3
  dimension: maintainability
  issue: The test suite cannot detect regression of the documentation claim.
  evidence:  tests/test_observability.py:1360 adds behavior tests but no assertion covers the factual content of the current_caller or server_busy_error docstrings; reverting either new explanation would pass the added tests.
  required_fix: Add a focused documentation contract check or an equivalent static validation covering the OAuth primary-key and four-scope statements.

## summary
The allowlist and both refusal paths otherwise preserve the intended scope values and exclude payload content. The unguarded scope lookup is a tool-result-breaking robustness defect.
