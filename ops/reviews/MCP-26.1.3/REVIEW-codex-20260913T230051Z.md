# REVIEW: MCP-26.1.3 by codex

- change-id:  MCP-26.1.3
- reviewer:   { pilot: codex, model: codex/gpt-5.6-luna }
- timestamp:  2026-09-13T23:00:51Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN
- resource-efficiency:     PASS
- business-logic-vs-spec:  PASS
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S2
  dimension: correctness
  issue: Caller attribution can use transport state from the request that opened a session instead of the request carrying the call.
  evidence:  sugra_api_mcp/server.py:560-579: the no-scope branch reads http_transport_ctx to assign transport and auth, although this is middleware ContextVar state inherited by session tasks rather than facts from the current SDK request.
  required_fix: Derive transport and auth from the current SDK message context or an explicitly per-message request association; do not consult http_transport_ctx for caller attribution.
- severity: S3
  dimension: correctness
  issue: Valid plain dotted versions such as 123456 are silently dropped by an unstated length restriction.
  evidence:  sugra_api_mcp/observability.py:341-342 and 377-381: _VERSION_RE limits every numeric part to five digits and _VERSION_MAX limits the complete value to 23 characters, although the stated contract only limits the number of ASCII-digit parts to four.
  required_fix: Accept any nonempty ASCII-digit part with at most four parts, or document and separately specify an explicit per-part and total-length limit in the contract.
- severity: S2
  dimension: maintainability
  issue: A mutation that labels every no-scope call as streamable_http with auth none would survive the added tests.
  evidence:  tests/test_request_credentials.py:222-303: the added integration test covers only HTTP calls with a request scope, while no test constructs a local or no-scope SDK context and exercises the fallback branch.
  required_fix: Add a real local/no-scope dispatch test, including inherited HTTP ContextVar state, and assert local transport and auth remain local; add an actual refused-call path test.

## summary
The fixed-class sanitization and normal HTTP per-request assertions are sound, and no user id or raw credential text is attached. The ContextVar fallback violates the per-call isolation contract and the test suite does not protect the local path.
