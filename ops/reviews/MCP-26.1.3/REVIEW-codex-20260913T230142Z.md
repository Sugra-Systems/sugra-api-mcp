# REVIEW: MCP-26.1.3 by codex

- change-id:  MCP-26.1.3
- reviewer:   { pilot: codex, model: codex/gpt-5.6-luna }
- timestamp:  2026-09-13T23:01:42Z
- verdict:    BLOCK

## dimensions
- correctness:             CONCERN
- resource-efficiency:     PASS
- business-logic-vs-spec:  PASS
- security:                PASS
- maintainability:         CONCERN
- operational-cost:        PASS

## findings
- severity: S2
  dimension: correctness
  issue: Malformed bracketed Host values can produce an incorrect loopback caller attribute.
  evidence:  sugra_api_mcp/observability.py:359-364: _host_class truncates any bracketed host at the first closing bracket and ignores the remainder, so values such as [::1]:not-a-port and [::1]evil are classified as loopback instead of other.
  required_fix: Parse bracketed IPv6 authorities strictly, accepting only the bare bracketed address or a numeric port suffix, and classify malformed suffixes as other. Add tests for malformed IPv6 lookalikes.
- severity: S2
  dimension: maintainability
  issue: The added end-to-end coverage does not detect regression of unauthenticated HTTP attribution.
  evidence:  tests/test_request_credentials.py:268-277: the real same-session HTTP calls all carry either an OAuth bearer or a sugra_ API key; the public unauthenticated path is only tested for request state, not for a tool span's mcp.caller.auth value.
  required_fix: Add a real HTTP tools/call without authorization and assert mcp.caller.auth is none, so a mutation of the no-principal fallback to api_key cannot survive.

## summary
Round 1 finding (1) is resolved because current_caller_facts no longer reads the middleware ContextVar and the same-session HTTP test varies request facts. Finding (2) is resolved because the bounds are now explicitly stated as contract. Finding (3) is resolved by the real SDK local dispatch and real admission-refusal tests. The new malformed IPv6 classification defect and the unauthenticated HTTP test gap require changes.
